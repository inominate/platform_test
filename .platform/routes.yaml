http://www.{default}/:
  to: http://{default}/
  type: redirect
http://{default}/:
  cache:
    enabled: true
  ssi:
    enabled: false
  type: upstream
  upstream: php:php
http://{default}/foo:
  to: http://{default}/bar
  type: redirect
